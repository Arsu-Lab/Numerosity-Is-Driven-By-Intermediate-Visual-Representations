target_transform_object_differentiation = {
    str([0, 0, 1]): "Camera",
    str([0, 1, 0]): "Different/None",
    str([1, 0, 0]): "Horse",
}
target_transform_numerosity = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_numerosity_background_change = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_numerosity_object_change = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_numerosity_size_difference = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_varying_size = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_varying_size_4val = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_varying_size_fixed_image_fixed_between = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_varying_size_fixed_image_fixed_between_4val = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_varying_size_and_object = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_varying_size_and_object_4val = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_varying_size_and_object_and_background = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_varying_size_and_object_and_background_4val = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_fill_objects_numerosity = {
    str([0, 0, 1]): ">",
    str([0, 1, 0]): "-",
    str([1, 0, 0]): "<",
}

target_transform_category_differentiation = {
    str([0, 0, 1]): "tools",
    str([0, 1, 0]): "animals",
    str([1, 0, 0]): "different/None",
}

target_transform_even_odd = {
    str([0, 0, 1]): "odd",
    str([0, 1, 0]): "even",
    str([1, 0, 0]): "different",
}

target_transform_target_object = {
    str([0, 0, 1]): "second",
    str([0, 1, 0]): "none",
    str([1, 0, 0]): "first",
}

target_transform_target_number = {
    str([0, 0, 1]): "second",
    str([0, 1, 0]): "same",
    str([1, 0, 0]): "first",
}

target_transform_fixed_object = {
    str([0, 0, 1]): "tools",
    str([0, 1, 0]): "animals",
    str([1, 0, 0]): "different/None",
}
